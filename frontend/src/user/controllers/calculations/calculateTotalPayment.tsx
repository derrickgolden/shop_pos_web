
export const calculateTotalPayments = (orders: [], orderDetails: []) =>{
    // orders.reduce((order.price, 0) => )
    // console.log(arrayToMap(orders));
    
}

// function arrayToMap(array :[]) {
//     return array.reduce((result, item) => {
//       const { medicine_id, ...rest } = item;
//       result[medicine_id] = { medicine_id, ...rest };
//       return result;
//     }, {});
//   }